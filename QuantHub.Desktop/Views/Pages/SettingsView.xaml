<UserControl x:Class="QuantHub.Desktop.Views.Pages.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel MaxWidth="640" HorizontalAlignment="Left">

        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="APPEARANCE" Style="{StaticResource CardTitleStyle}"/>
                <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource PanelBorderBrush}"
                        BorderThickness="1" CornerRadius="10" Padding="3" HorizontalAlignment="Left">
                    <ItemsControl ItemsSource="{Binding ThemeOptions}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Label}" Padding="18,8" Margin="2,0" MinWidth="90"
                                        FontSize="13" FontWeight="SemiBold" BorderThickness="0"
                                        HorizontalContentAlignment="Center"
                                        Command="{Binding DataContext.SelectThemeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Value="True">
                                                    <DataTrigger.Binding>
                                                        <MultiBinding Converter="{StaticResource IsEqual}">
                                                            <Binding Path="Theme"/>
                                                            <Binding Path="DataContext.Theme" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                        </MultiBinding>
                                                    </DataTrigger.Binding>
                                                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="DIFFICULTY MODE" Style="{StaticResource CardTitleStyle}"/>
                <ItemsControl ItemsSource="{Binding ViewModeOptions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Padding="16,12" Margin="0,0,0,8" HorizontalContentAlignment="Left" Cursor="Hand"
                                    Command="{Binding DataContext.SelectViewModeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Value="True">
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource IsEqual}">
                                                        <Binding Path="Mode"/>
                                                        <Binding Path="DataContext.ViewMode" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Label}" FontWeight="Bold" FontSize="14"
                                               Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap"
                                               Foreground="{DynamicResource MutedTextBrush}" Margin="0,3,0,0" HorizontalAlignment="Left"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="AI RESEARCH" Style="{StaticResource CardTitleStyle}"/>
                <TextBlock Text="Your Claude API key, used by the AI Research chat page. Stored locally only."
                           TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedTextBrush}" Margin="0,0,0,10"/>
                <TextBox Text="{Binding ClaudeApiKey, UpdateSourceTrigger=PropertyChanged}"
                         CharacterCasing="Normal" FontFamily="Consolas" Width="420" HorizontalAlignment="Left"
                         ToolTip="sk-ant-..."/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
